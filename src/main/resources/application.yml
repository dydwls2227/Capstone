server:
  port: 8080
spring:

  jpa:
    hibernate:
      ddl-auto: create # 애플리케이션 실행 시 스키마 자동 생성
    properties:
      hibernate:
        show_sql: true # SQL 쿼리 로그 출력
        format_sql: true
  profiles:
    include: oauth
